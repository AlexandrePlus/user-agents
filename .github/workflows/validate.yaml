# inspired by https://docs.github.com/en/actions/guides/building-and-testing-python
# can to be run locally (in addition to Github) with "act" : act -s GITHUB_TOKEN=[github token with public_repo scope]
name: Validate JSON

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [2.7, 3.9]
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
      - name: Display Python version
        run: python -c "import sys; print(sys.version)"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r pip-test-requirements.txt
      - name: Test with pytest
        run: |
          python -m pytest
